@if (Request.IsAuthenticated)
{
    <div id="sidebar" class="sidebar responsive">
        <ul class="nav nav-list" id='mainmenu'>
            @if (User.IsInRole("Admin"))
            {
                <li class="active menuitem">
                    <a href="@Url.Action("Dashboard", "Admin")">
                        <i class="menu-icon fa fa-tachometer"></i>
                        <span class="menu-text"> Dashboard </span>
                    </a>

                    <b class="arrow"></b>
                </li>
            }
            @if (User.IsInRole("Admin"))
            {
                <li id="liInstructor" class="menuitem">
                    <a href="@Url.Action("Index", "Instructor")">
                        <i class="menu-icon fa fa-graduation-cap"></i>
                        <span class="menu-text">
                            Instructors
                        </span>
                    </a>
                    <b class="arrow"></b>
                </li>
            }
            <li class="menuitem">
                <a href="@Url.Action("Index","Student")" class="dropdown-toggle">
                    <i class="menu-icon fa fa-users"></i>
                    <span class="menu-text">
                        Students
                    </span>
                </a>

                <b class="arrow"></b>
            </li>
            <li class="menuitem">
                <a href="@Url.Action("Calendar", "Instructor")">
                    <i class="menu-icon fa fa-calendar"></i>

                    <span class="menu-text">
                        Calendar
                    </span>
                </a>

                <b class="arrow"></b>
            </li>
            @if (User.IsInRole("Admin"))
            {
                <li class="menuitem">
                    <a href="@Url.Action("InstructorsDayCalendar", "Instructor")">
                        <i class="menu-icon fa fa-calendar"></i>

                        <span class="menu-text">
                            Instructors Day Calendar
                        </span>
                    </a>

                    <b class="arrow"></b>
                </li>
            }
            @if (User.IsInRole("Admin"))
            {
                <li id="" class="menuitem">
                    <a href="@Url.Action("Packages", "Instructor")">
                        <i class="menu-icon fa fa-diamond"></i>
                        <span class="menu-text">
                            Packages
                        </span>
                    </a>

                    <b class="arrow"></b>
                </li>
                <li id="" class="menuitem">
                    <a href="@Url.Action("Area", "Instructor")">
                        <i class="menu-icon fa fa-university"></i>
                        <span class="menu-text">
                            Areas
                        </span>
                    </a>

                    <b class="arrow"></b>
                </li>
                <li id="" class="menuitem">
                    <a href="@Url.Action("AllLeaves", "Instructor")">
                        <i class="menu-icon fa fa-list"></i>
                        <span class="menu-text">
                            All Leaves
                        </span>
                    </a>

                    <b class="arrow"></b>
                </li>
            }
            <li class="menuitem">
                <a href="@Url.Action("InstructorLeaves", "Instructor")">
                    <i class="menu-icon fa fa-list"></i>

                    <span class="menu-text">
                        Apply Leave
                    </span>
                </a>

                <b class="arrow"></b>
            </li>
        </ul><!-- /.nav-list -->
        <!-- #section:basics/sidebar.layout.minimize -->
        <div class="sidebar-toggle sidebar-collapse" id="sidebar-collapse">
            <i class="ace-icon fa fa-angle-double-left" data-icon1="ace-icon fa fa-angle-double-left" data-icon2="ace-icon fa fa-angle-double-right"></i>
        </div>

        <!-- /section:basics/sidebar.layout.minimize -->
        <script type="text/javascript">
            try {
                ace.settings.check('sidebar', 'collapsed');
            } catch (e) {
            }
        </script>


    </div>
}